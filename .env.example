# Frontend (Next.js)
# If you run the backend separately, you can point directly at it (not required when using Next.js /api proxy).
NEXT_PUBLIC_API_URL=
# Optional: disable all API calls (uses in-memory board defaults)
NEXT_PUBLIC_API_DISABLED=false

# Next.js dev proxy target for /api/* (recommended for cookies + Docker)
API_PROXY_TARGET=http://localhost:3000

# Backend (Express)
PORT=3000
# Comma-separated list. Include 0.0.0.0 if you open the frontend at http://0.0.0.0:5173
CORS_ORIGIN=http://localhost:5173,http://0.0.0.0:5173

# Postgres (when running locally outside Docker)
PGHOST=localhost
PGUSER=myuser
PGPASSWORD=mypassword
PGDATABASE=mydb
PGPORT=5433
